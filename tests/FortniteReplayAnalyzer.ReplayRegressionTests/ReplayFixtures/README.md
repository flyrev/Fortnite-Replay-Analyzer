# Replay Fixtures

Add replay fixtures here.

- `sample-match.replay`
- `sample-match.json`

The `.json` file is optional. If it is missing, the harness still checks that the replay can be parsed and analyzed without crashing.

You can also point the test project at another folder by setting `FORTNITE_REPLAY_FIXTURES_DIR`.
